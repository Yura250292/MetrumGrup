-- «АІ покращення» текстової наради: noteRefined — вичищена/відформатована
-- версія нотатки (виправлені помилки, гарне оформлення). Окрема копія;
-- оригінал noteText лишається недоторканим.

ALTER TABLE "meetings" ADD COLUMN "noteRefined" TEXT;
